# Core dependencies (required for basic functionality)
click>=8.0.0,<9.0.0
rich>=13.0.0,<14.0.0

# Optional dependencies are NOT listed here.
# Install them via pip install prompt-mirror[extra]:
#   pip install -e ".[viz]"      # For visualization (matplotlib)
#   pip install -e ".[pdf]"      # For PDF export (reportlab)
#   pip install -e ".[all]"      # For all optional features

# For development
# pytest>=7.0.0
# black>=23.0.0
# pip-audit  # For security auditing
